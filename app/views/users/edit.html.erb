<div>
  <h1 class="font-bold text-2xl mb-3">Editing User</h1>

  
  <turbo-frame id="<%= dom_id @user %>">
    <%= render "form", user: @user %>
    <%= link_to "Never Mind", users_path, class: "btn mb-3 bg-gray-100" %>
  </turbo-frame>
</div>
